# ── Smart Shopping Cart – Raspberry Pi Requirements ──────────────────────────
# Install with:  pip install -r requirements_pi.txt --break-system-packages

# Core UI & image handling
Pillow>=10.0.0

# QR Code generation (for payment page)
qrcode[pil]>=7.4

# Barcode scanning via camera
opencv-python-headless>=4.8.0   # use headless build on Pi (no GUI dep)
pyzbar>=0.1.9

# Payment gateway
razorpay>=1.3.0

# Backend / cloud sync
supabase>=2.0.0
python-dotenv>=1.0.0

# HX711 Load Cell (Raspberry Pi ONLY)
# Uncomment the line below on Pi; leave commented on Windows/Mac
# hx711>=1.1.2.3

# GPIO (Raspberry Pi ONLY – pre-installed on Raspbian)
# RPi.GPIO  # already available; no need to pip install on Raspbian
